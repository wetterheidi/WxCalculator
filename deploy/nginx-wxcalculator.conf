server {
    listen 80;
    server_name wxcalculator.wetterheidi.de;

    root /apps/wxcalculator;
    index WxCalculator.html;

    auth_basic "Restricted";
    auth_basic_user_file /etc/nginx/.htpasswd-wetterheidi;

    location / {
        try_files $uri $uri/ =404;
    }
}
